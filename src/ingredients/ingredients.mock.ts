import mongoose from "mongoose";
import { IIngredients } from "./ingredients.types";

export const getMockedMovements = (): IIngredients[] => ([
  {
    description: 'A beatiful apple',
    name:
  },
])